export type Resource = {
  title: string;
  description: string;
  tags?: string[];
  link: string;
};

export type Metadata = {
  title: string;
  description: string;
  image: string;
  link: string;
};
